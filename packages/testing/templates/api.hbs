import { HttpClient } from './../../scripts/client';

describe('{{title}} module test', () => {

    beforeAll(async () => {
        await HttpClient.initDatabase()
    })

    afterAll(async () => {

    })

{{#each methods}}

    it('{{path}}', async () => {
{{#each parameters}}
     {{#if children}}
        const {{name}} = {
            {{#each children}}
            {{name}}: '',
            {{/each}}
        };
     {{else}}
         const {{name}} = '';
     {{/if}}
{{/each}} 
    const res = await HttpClient.{{tag}}Api.{{operationId}}({{paramsNames}});
        expect(res.status).toBe({{state}});
    });
    
{{/each}}
});